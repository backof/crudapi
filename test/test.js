var test = require('unit.js');
var str = 'Hello, world!';

describe('CRUD Test', function() {
  it('create', function() {
    
        test.string('test').contains(
            'test');
  });
  
  it('read', function() {
    
        test.string('test').contains(
            'test');
  });
  
   it('upadte', function() {
    
        test.string('test').contains(
            'test');
  });
  
   it('delete', function() {
    
        test.string('test').contains(
            'test');
  });
  
});